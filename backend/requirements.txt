fastapi>=0.115.6
uvicorn[standard]>=0.32.1
python-multipart>=0.0.18
websockets>=13.1

pdfplumber>=0.11.4
pdfminer.six>=20231228
pypdf>=4.0.0

pillow>=11.0.0

openpyxl>=3.1.5

pandas>=2.3.0
python-dateutil>=2.9.0

python-dotenv>=1.0.1
pydantic>=2.10.3
pydantic-settings>=2.7.0

aiofiles>=24.1.0

# ── Optional: only needed for scanned/image PDFs (rare for freight contracts) ─
# Omit these if RAM < 8 GB or you want a fast install.
# docling installs PyTorch (~1.5 GB) — skip unless you need OCR fallback.
# docling>=2.0.0
# easyocr>=1.7.0
# pdf2image>=1.17.0    # also needs: poppler system package
# pytesseract>=0.3.13  # also needs: tesseract-ocr system package
